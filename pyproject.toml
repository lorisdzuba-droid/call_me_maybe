[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "pydantic>=2.13.0",
    "llm-sdk",
    "tqdm",
    "accelerate>=1.13.0",
    "flake8>=7.3.0",
    "mypy>=1.20.2",
]

[tool.uv.sources]
llm-sdk = { path = "llm_sdk"}

[[tool.mypy.overrides]]
module = "llm_sdk.*"
ignore_errors = true
